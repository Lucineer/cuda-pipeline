[package]
name = "cuda-pipeline"
version = "0.1.0"
edition = "2021"
description = "Multi-stage processing pipeline with backpressure and stage monitoring."
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
