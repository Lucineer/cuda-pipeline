[package]
name = "cuda-pipeline"
version = "0.1.0"
edition = "2021"
description = "Data pipeline — ETL stages, transforms, filters, mappers, reducers, and stream processing for agent data flows"

[dependencies]
serde = { version = "1", features = ["derive"] }
